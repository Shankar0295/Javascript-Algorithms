//16/12/2021
// 54.Write a function that uses the ternary operator to return 'yeah' if bool is true, and 'nope' otherwise.
console.log("54.Write a function that uses the ternary operator to return 'yeah' if bool is true, and 'nope' otherwise.")

const ternary = (isTrue) => {
    return (isTrue) ? 'yeah' : 'nope'
}

ternary(true)
ternary(false)